.DS_Store
.playwright-mcp/
*.log
*.tmp

# Local preview captures
uture-*-fixed.png
uture-logo-fix*.png


# Local Claude Code config — ignore everything except project skills
.claude/*
!.claude/skills/

# Installed agent skills: keep markdown, drop any deps
.agents/**/node_modules/

# 원본 강의 사진 (블러 처리 전 — 얼굴·과정명 노출, 절대 커밋 금지)
images/
